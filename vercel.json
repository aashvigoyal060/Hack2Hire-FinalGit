{
  "installCommand": "cd frontend && npm install",
  "buildCommand": "node scripts/prepare-deploy.mjs && cd frontend && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://hack2hire-backend-production.up.railway.app/api/:path*"
    },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
